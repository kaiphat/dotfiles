### PRIMARY KEY ###
unbind C-b
set -g prefix C-a
bind C-a send-prefix

### SETUP ###
set -g mouse on
set -g base-index 1
set -g pane-base-index 1
set -g allow-rename on
set -g renumber-windows on
setw -g mode-keys vi
set -sg escape-time 0
setw -g aggressive-resize on

# set -g default-terminal "xterm-kitty"
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*256col*:RGB"
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'

set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence off
set-window-option -g monitor-activity off
set-option -g bell-action none

### CONFIG ###

# colors #
DARK="#202837"
CYAN="#a3b8ef"
LIGHT_BLUE="#9398cf"
LIGHTEST_BLUE="#404060"

# formats #
DATE_FORMAT="%Y-%m-%d"
TIME_FORMAT="%H:%M:%S"

S1="#[fg=${LIGHTEST_BLUE},bg=${CYAN}]"
S2="#[fg=${CYAN},bg=${LIGHTEST_BLUE}]"
S3="#[fg=${LIGHT_BLUE},bg=${LIGHTEST_BLUE}]"
S4="#[fg=${CYAN},bg=default]"
RIGHT_FULL=""
LEFT_FULL=""
LEFT=""
RIGHT=""

### STATUS LINE ###
set -g status-interval 1
set -g status-position top
set -g status-justify left
set -g status-left-length 50
set -g status-right-length 140
set -g status-style default
set -g message-style "fg=${LIGHTEST_BLUE},bg=${CYAN}"
set -g message-command-style "fg=${LIGHTEST_BLUE},bg=${CYAN}"
set -g status-left ""
set -g status-right "${S4}${LEFT_FULL}${S1} ${DATE_FORMAT} ${TIME_FORMAT} "
set -g window-status-format "${S2} #I:#(echo #{pane_current_path} | sed "s/.home.ipunko/~/g" | rev | cut -d'/' -f-2 | rev) "
set -g window-status-current-format "${S1} #I:#(echo #{pane_current_path} | sed "s/.home.ipunko/~/g" | rev | cut -d'/' -f-2 | rev) "
set -g window-status-separator ""
set -g mode-style "fg=${LIGHTEST_BLUE},bg=${CYAN}"

### KEY BINDINGS ###
bind r source-file ~/.tmux.conf \; display ".tmux.conf reloaded"

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind -r H resize-pane -L 10
bind -r J resize-pane -D 10
bind -r K resize-pane -U 10
bind -r L resize-pane -R 10

bind / copy-mode \; send-key ?
bind [ select-layout tiled
bind d copy-mode
bind D kill-session

bind c new-window -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"
bind o split-window -v -c "#{pane_current_path}"

bind x kill-window -a
bind q kill-pane -a

# bind C-m split-window -h -c "#{pane_current_path}" \; swap-pane -s 1 -t 2 \; resize-pane -L 50
# https://gist.github.com/sdondley/b01cc5bb1169c8c83401e438a652b84e Super Guide to the split-window tmux
bind C-m split-window -p 32 -b -h -c "#{pane_current_path}"
# 4 panes
bind C-l split-window -p 50 -c "#{pane_current_path}" -h \;\
         select-pane -t 1 \;\
         split-window -p 50 -c "#{pane_current_path}" \;\
         select-pane -t 1 \;\
         split-window -p 50 -c "#{pane_current_path}" -h \;\
         select-pane -t 1 \;\
         select-layout tiled
# 6 panes
bind C-k split-window -p 50 -c "#{pane_current_path}" -h \;\
         select-pane -t 1 \;\
         split-window -p 50 -c "#{pane_current_path}" \;\
         select-pane -t 1 \;\
         split-window -p 50 -c "#{pane_current_path}" -h \;\
         select-pane -t 1 \;\
         split-window -p 50 -c "#{pane_current_path}" \;\
         select-pane -t 1 \;\
         split-window -p 50 -c "#{pane_current_path}" -h \;\
         select-pane -t 1 \;\
         select-layout tiled

bind C-j split-window -p 32 -c "#{pane_current_path}" -b -h \;\
         split-window -p 50 -c "#{pane_current_path}" \;\
         select-pane -t 1 \;\
         split-window -p 50 -c "#{pane_current_path}" \;\
         select-pane -t 3 \;\
         split-window -p 50 -c "#{pane_current_path}" \;\
         select-pane -t 5 \;\
         split-window -p 50 -c "#{pane_current_path}" \;\
         select-pane -t 1 \;\
         select-pane -t 5

bind C-i split-window -p 32 -c "#{pane_current_path}" -b -h \;\
         select-pane -t 1 \;\
         split-window -p 50 -c "#{pane_current_path}" \;\
         select-pane -t 2 \;\
         split-window -p 50 -c "#{pane_current_path}" \;\
         select-pane -t 1 \;\
         split-window -p 50 -c "#{pane_current_path}" \;\
         select-pane -t 1 \;\
         select-pane -t 5

bind -n M-n previous-window
bind -n M-p next-window
bind -n M-z resize-pane -Z 
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

bind -n M-i new-window -c "#{pane_current_path}"
bind -n M-o split-window -h -c "#{pane_current_path}"
bind -n "M-'" split-window -v -c "#{pane_current_path}"

bind -n M-x kill-window
bind -n M-q kill-pane

bind -n -r M-s swap-pane -D

### PLUGINS ###
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
# set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @continuum-boot on
set -g @continuum-restore on
set -g @continuum-save-interval 0
set -g @continuum-boot-options 'kitty,fullscreen'

set -g @resurrect-strategy-nvim 'session'

set -g @yank_action 'copy-pipe'

run '~/.tmux/plugins/tpm/tpm'

### AUTOSTART ###
# if not set -q TMUX
#     set -g TMUX tmux new-session -d -s base
#     eval $TMUX
#     tmux attach-session -d -t base
# end
